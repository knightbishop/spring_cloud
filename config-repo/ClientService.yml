spring:
  application:
    name: ClientService

server:
  port: 8807

eureka:
  client:
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true

